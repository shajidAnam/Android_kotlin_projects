<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment_11">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment_11"
        android:usesCleartextTraffic="true">
        <activity android:name=".feature.student.view.Student_details_activity"></activity>
        <activity android:name=".feature.student.view.StudentListActivity" />
        <activity android:name=".feature.login.view.LoginActivity" />
        <activity android:name=".feature.web_view.WebUrlActivity" />
        <activity android:name=".feature.home.view.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>